{% assign course = include.course %}
<div class="col-12 col-md-6 mb-4">
  <a class="course-card-link" href="{{ course.url | relative_url }}">
    <article class="course-card h-100">
      <div class="course-card-meta">
        <span>{{ course.semester }}</span>
        {% if course.status == 'active' %}<span class="course-status">em andamento</span>{% endif %}
      </div>
      <h2>{{ course.title }}</h2>
      <p class="course-program">{{ course.program }} · {{ course.institution }}</p>
      <p>{{ course.description }}</p>
      <span class="course-card-action">Acessar disciplina →</span>
    </article>
  </a>
</div>
